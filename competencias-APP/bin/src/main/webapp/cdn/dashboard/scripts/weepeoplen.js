/**
 * Enlazamos el componente a BiganStructure
 * @returns
 */   
   
if (BiganStructure) {  
	BiganStructure.linkContext('{frame_id}', 
								'{url}', 
								biganPeopleNChart);
}

//$('#peoptit{frame_id}').html(d.context);
